# TravelOS — Build progress

Working agreement (owner, 2026-09-18): build all phases continuously on `claude/travelos-operating-system-581b62`; commit after every module; automated tests must pass before a phase is marked done; never merge to `main`; never touch the production database — all schema work happens on a Neon branch or the disposable local Postgres used by the test suite; the owner does full manual testing at the end.

Status legend: ☐ not started · ◐ in progress · ☑ done (tests green) · ⊘ blocked · ⇢ deferred (with the phase it moved to)

## How to run the checks

```bash
npm run typecheck && npm run typecheck:server      # both must be clean
npm run test:unit                                   # no database needed
TEST_DATABASE_URL=postgresql://travelos:travelos@localhost:5434/travelos_test npm run test:integration
```

The integration database is a disposable container: `docker run -d --name travelos-pg -e POSTGRES_PASSWORD=travelos -e POSTGRES_USER=travelos -e POSTGRES_DB=travelos_test -p 5434:5432 postgres:16-alpine`. The suite refuses any host that is not local or any database whose name lacks `test` (`tests/integration/helpers/guard.ts`), so it cannot be pointed at production by mistake. CI (`.github/workflows/ci.yml`) runs the same commands against a Postgres service container.

New schema change recipe (never `db push`): edit `prisma/schema.prisma` → deploy the current chain to a scratch database (`travelos_dev`) → `npx prisma migrate diff --from-url <dev> --to-schema-datamodel prisma/schema.prisma --script > prisma/migrations/<timestamp>_<name>/migration.sql` → hand-edit if data/view statements are needed → `migrate deploy` on the scratch database → `tests/integration/migrations.test.ts` must stay green.

## Phase 1 — Foundation

| # | Module | Status | Commit | Notes |
|---|---|---|---|---|
| 1.0 | Pre-Phase-1 security hotfix + credential rotation | ☑ | `05ba30a8` | 49/49 role probes |
| 1.1 | Tooling: vitest, supertest, server typecheck in build, CI workflow | ☑ | `4ac18b28` | `db:push`/`db:reset` scripts removed |
| 1.2 | Fix the remaining server type errors | ☑ | `4ac18b28` | 73 → 0 |
| 1.3 | Unit tests: finance, GST, task engine, ids, permissions (server = client), redaction | ☑ | `4ac18b28` | 59 tests |
| 1.4 | Integration tests: invoice/credit-note service, finance service | ☑ | `bbe1a9fd` | 19 tests on a real Postgres |
| 1.5 | API permission-matrix tests (role × route, redaction, write-side guards) | ☑ | `bbe1a9fd` | 32 tests |
| 1.6 | Migration baseline: drift adopted, 14 unreplayable folders squashed into one generated baseline (+ ledger view), migration-chain test, production catch-up SQL and runbook | ☑ | `bbe1a9fd` | Production execution is the owner's step at cutover (RUNBOOK) |
| 1.7 | Tenancy: `Organization`, `organizationId` on all 35 models, tenant-scoped Prisma client (`core/tenant.ts`), org in session, per-org numbering + company settings, ledger view carries org | ☑ | (this commit) | Nested writes rely on the column default until Phase 9 removes it; raw SQL must filter explicitly |
| 1.8 | Core: request context (request id, actor, source), error envelope (`core/errors.ts`), zod validation middleware, audit writer with source/request id, `/api/v2/me` | ☑ | (this commit) | RBAC tables + custom roles ⇢ Phase 9; the static catalogue is served by `/api/v2/me` |
| 1.9 | Server-side sessions with instant revocation (logout, deactivation, role change, password reset), 30-day idle expiry, login rate limit per (IP, email), API ceiling, helmet, CORS by environment | ☑ | (this commit) | Sessions cached 60 s in-process; revocation clears the cache |
| 1.10 | Job runner: `jobs` table, claim with `FOR UPDATE SKIP LOCKED` + lease, retries with backoff, per-organisation handler context, recurring system jobs (scheduler rules every 15 min, outbox dispatch every minute), `POST /api/jobs/tick` behind `CRON_SECRET`, daily safety-net Vercel cron, local dev loop | ☑ | (this commit) | Minute-level scheduling: Pro cron or an external scheduler (decision 6) |
| 1.11 | Storage + documents: S3/R2 provider with presigned PUT/GET, local-disk provider for dev/tests behind HMAC-signed URLs, `documents` + `document_links`, `/api/v2/documents` (register → upload → complete → list/link/download → guarded delete), 503 NOT_CONFIGURED in production without a bucket | ☑ | (this commit) | Phase 5 adds classification/extraction on top |
| 1.12 | Repo hygiene: Supabase-era code, unrouted auth pages, third permission vocabulary, legacy vanilla-JS app, junk files removed; `@supabase/supabase-js` dropped; README rewritten | ☑ | (this commit) | Production Vite build verified |
| 1.13 | Design system tokens + primitives, new shell navigation | ⇢ 2.1 | | Built as the first Phase 2 module so it serves real screens |

## Phase 2 — Core CRM + Sales
| # | Module | Status | Commit | Notes |
|---|--------|--------|--------|-------|
| 2.1 | Design system primitives (`src/design-system/`), typed API client (`src/lib/api.ts`), TravelOS navigation groups, v2 pages carry their own PageHeader | ☑ | | Legacy screens untouched; superseded pages move under `/legacy/*` as v2 replaces them |
| 2.2 | Customers v2: server-side list/search/pagination, `phoneNormalized` dedupe (block + force), duplicate groups + merge, relationships, referrals, tags, soft delete with guards, Customer 360 (trips · sales · finance · travellers · documents · activity) | ☑ | | Migration `20260918140000_customers_v2` backfills normalised phones, corporate flag, and seeds the `CUS` sequence. Legacy page at `/legacy/customers` |
| 2.3 | Travellers: Prisma model `Traveller` (table `passengers` kept), title/relation/contact/domestic-ID columns, `trip_travellers` membership (roles LEAD/ADULT/CHILD/INFANT, mirrored into `trips.passengerIds` + `pax` both ways), PAX display ids, passport duplicate guard, list/search/passport filters, passport-alerts endpoint, scheduler rule → task for expired / <6-month passports on trips within 90 days; SPA list + detail + form, Customer 360 tab | ☑ | | Migration `20260918150000_travellers` backfills membership from `passengerIds` and seeds the PAX sequence. Trip-side traveller picker lands with the Phase 3 trip control centre |
| 2.4 | Leads v2: lifecycle new → contacted → qualified → converted / lost (reason required), server L-YYYY-NNNN ids, duplicate check against open leads + customers, assignment to active users (`assignedToUserId`), timeline + audit, convert → customer (phone-matched or created) + enquiry; board UI with lead drawer | ☑ | | Legacy statuses mapped in migration `20260918160000_sales_pipeline_v2`; dashboard pipeline rows follow the new lifecycle |
| 2.5 | Enquiries v2: ENQ-YYYY-NNNN numbers, adults/children/infants (+ pax sum), rooms, origin, trip type, hotel category, meal plan, budget range, preferences, priority, lead link, inline customer creation deduped by phone, transitions (lost reason required, closed = read-only), assignment, notes, follow-up tasks, `GET /api/v2/me/team`; board + detail page | ☑ | | v1 `/api/enquiries` still works (adults kept in step); legacy pipeline at `/legacy/enquiries` |
| 2.6 | Unified quotation engine on `SalesQuote`: pricing bases PER_PERSON (adult/child/infant rates) / PER_UNIT / PER_ROOM / PER_GROUP, option groups (alternatives, one selected), parties (family-wise totals, pax-weighted share of shared items), GST EXCLUDED/INCLUDED/NONE with per-item override, pro-rata discounts, versions (`-vN`, one current), admin approval gate for margins below `QUOTE_MIN_MARGIN_PCT`, send/viewed/negotiating/rejected/expired/accept lifecycle wired to enquiry status, customer-safe view (no cost/margin/supplier/internal notes). Pure calc `src/shared/calc/quotation.ts` (10 unit tests) drives both builder and API. SPA: list, builder with live totals, detail with option picking, approval, versions, accept, customer preview | ☑ | | Migration `20260918170000_quotation_engine` (existing quotes keep totals; `GK-Q` numbering seeded). Legacy builders remain at `/sales-quotes` and `/quotations` |
| 2.7 | Booking contracts: `booking_contracts` + `payment_schedule_items`; created on quotation acceptance (whole group or one per party with pax-weighted shares of shared items), each with a legacy `Trip` + `TripService` rows + traveller membership so existing ops/finance screens work; default schedule (advance now, balance 15 days before departure; single instalment for short notice) editable with sum/order validation; instalment states from money received; lifecycle CONFIRMED → IN_PROGRESS → COMPLETED / CANCELLED synced to the trip; payments-due feed. SPA: Bookings list + detail (services, money, schedule editor, status) | ☑ | | Migration `20260918180000_booking_contracts` seeds the GK trip-id sequence; trip ids skip numbers the legacy counter already used |
| 2.8 | Data migration `20260918190000_migrate_legacy_quotations`: every legacy `quotations` row becomes an enquiry (`enq_legacy_<id>`) + v2 quotation (`sq_legacy_<id>`) with PER_UNIT items, status/approval/GST mapped, customer matched by phone or created; idempotent. The four existing sales quotes carried over in 2.6 (additive columns) | ☑ | | Verified by `tests/integration/legacy-quotation-migration.test.ts`, which seeds legacy rows and applies the SQL twice. Legacy screens stay under `/quotations` until the owner retires them |

## Phase 3 — Travel operations ☑
Branch `claude/travelos-phase-3-41f57b` (fast-forward of the Phase 0–2 branch). Complete; waiting for the owner's checkpoint test before Phase 4.

| # | Module | Status | Commit | Notes |
|---|--------|--------|--------|-------|
| 3.0 | Identity data protection (hard rule 7, gap left by Phase 2): AES-256-GCM field encryption (`core/crypto.ts`, `DATA_ENCRYPTION_KEY`), blind index for duplicate checks and exact search, masked `XXXX-XXXX-1234` in every API response (v2, legacy `/api/passengers`, `/api/customers`, `/api/data/all`), echoed masks mean "unchanged", audited reveal endpoints + "Show" control, Aadhaar accepted with Verhoeff validation, `identity.encrypt-legacy` job seals plaintext written before this change | ☑ | (this commit) | Migration `20260918200000_identity_encryption` is additive; plaintext columns stay until the platform phase drops them. Passport search is now an exact match (no substring) |
| 3.1 | Masters: vendors v2 (kinds, phone/GSTIN dedupe, bank details finance-only, VEN ids from the sequence), hotels + room types + non-overlapping season rates per meal plan + night-by-night stay quote, vehicles (insurance/permit/fitness/PUC expiries), drivers (licence expiry, languages), compliance board, activities (default adult/child prices), CSV/Excel import with a server-validated row-by-row preview, match-and-update, skip-invalid and one audit row per record. Pages: Suppliers, Hotels (+ detail with room types and seasons), Fleet (vehicles, drivers, papers), Activities | ☑ | (this commit) | Migration `20260918210000_operations_masters` backfills vendor kind and phone and seeds the VEN sequence. Rates and prices are hidden from OPERATIONS. Classic Suppliers & payables stay at `/vendors` |
| 3.2 | Operational records API (`/api/v2/ops`): hotel bookings (masters snapshot, default supplier, priced from the rate sheet with gap report, confirmation number required, re-confirmation when the stay changes), vehicle assignments (IST pickup times, vehicle/driver double-booking refused under row locks — race-tested, lapsed insurance/permit/fitness/PUC or licence refused, seat-count and missing-paper warnings, vendor-sent vehicle/driver details, reinstating re-checks), activity bookings (priced from the master), status machines, commercial redaction, audit on the trip, trip-change hooks for readiness and tasks. Trip `stage` column added (backfilled from the classic status) | ☑ | (this commit) | Migration `20260918220000_trip_operations`. Screens arrive with the trip control centre (3.4) |
| 3.3 | Tickets (`/api/v2/tickets`, Tickets pages): ticket → segments → one row per passenger per segment; flight/train/bus, PNR, carrier/train/bus number, class, quota, seat/coach/berth, per-segment and per-passenger boarding/dropping points, status derived from the rows (CONFIRMED / PARTIAL / WAITLISTED / RAC), IRCTC status strings parsed (unreadable ones refused, never guessed), bulk status paste after chart preparation, fare breakup with GST on the service fee from settings (verify with CA), cost/fee hidden from non-commercial roles, standalone tickets for walk-in customers. Classic bookings imported by an idempotent SQL function (flight/train/bus → tickets; hotel/cab/activity → 3.2 records when linked to a trip) at migration time, hourly by `legacy.bookings-import`, and from the Tickets page | ☑ | (this commit) | Migrations `20260918230000_tickets` and `20260918230100_import_legacy_bookings` (reverse SQL in its header; classic rows untouched). Visa/insurance/other stay on the classic screen. Classic cab bookings had no end time: 8 h is assumed and flagged in the notes |
| 3.4 | Trip control centre (`/api/v2/trips`, `/trips` workspace): one trip holds many family-party booking contracts (converting an enquiry can add a party to an existing tour); stage PLANNING → CONFIRMING → READY → ONGOING → COMPLETED / CANCELLED with transitions and readiness checks in `calc/tripStage.ts` (READY is blocked by missing dates/travellers, unconfirmed hotels/vehicles/activities, vehicles without drivers, tickets not booked, and missing passports on international tours; waitlisted tickets warn), row-locked stage changes with a reason for cancellation, READY drops back to CONFIRMING automatically (SYSTEM audit) when a booking stops being confirmed, classic `status` follows the stage and vice versa; ordered pickup points with IST times, party and pickup point per traveller; workspace tabs Overview (readiness, money, parties, open tasks) / Travellers / Tickets / Hotels / Transport (live conflict check) / Activities / Documents (private upload, short-lived links) / Timeline. Display ids skip numbers already taken by classic rows (`nextFreeDisplayId`) | ☑ | (this commit) | Migration `20260919000000_trip_control_centre` drops the one-contract-per-trip unique index and backfills party and pickup links. Contract statuses now follow the trip; a contract can only be cancelled directly. Per-party receipts wait for Phase 4, so shared-tour contracts show amounts due without a paid status (`paymentTracking: TOUR`). Classic trip screens at `/legacy/trips` |
| 3.5 | Itinerary v2 (`/api/v2/itineraries`, trip workspace → Itinerary tab): one itinerary per trip, days follow the trip dates; start blank, from the trip's bookings, or by copying a previous run of the tour (manual content only); per day: title, morning/afternoon/evening, night's stay, meals, transport, customer notes, **internal notes**, and timed items (time, kind, customer text, **internal note**, **internal cost** — commercial roles only, hide-from-customer). "Sync with bookings" turns hotels, vehicle duties, activities and ticket legs into items with generated customer-safe text (never vendor, cost or internal notes), moves/removes them when bookings change, fills each night's hotel and re-dates days when the trip dates move. Saves are revision-guarded (409 on a stale editor); booking items cannot be retyped or deleted, only annotated or hidden. Customer copy (`/:id/customer`) is an explicit allowlist (`toCustomerItinerary`) used by the navy-and-gold print page (`/print/trip-itinerary/:id`) and the WhatsApp text (copied, never sent). "Mark as shared" records which version the customer has; readiness warns when there is no itinerary, it was never shared, or it changed since. Classic itineraries open in the builder and keep their activity lines on the first save; the classic builder refuses to overwrite or delete a v2 itinerary | ☑ | (this commit) | Migration `20260919010000_itinerary_v2` is additive (reverse SQL in its header) and seeds the ITN sequence. Tests prove internal notes, costs and hidden items never reach the customer copy (unit + API) |
| 3.6 | Task engine (`/api/v2/tasks`, `/api/v2/task-rules`, **Today** page, **Task rules** settings): 15 rules in `calc/taskRules.ts` — flight web check-in, group seat selection, Tatkal window (AC / non-AC hour), daily waitlist/RAC check, train chart, passport validity (and missing passports on international trips), visa deadline, hotel / vehicle / activity confirmation pending, driver details missing, balance to collect, lead and quotation follow-ups, post-trip feedback. Timings and on/off per rule in the `task_rules` table (admin only, range-checked, defaults documented with "verify" notes; saving recalculates at once). Every trip write recalculates that trip's tasks in the same transaction (trip-change hook); `tasks.sweep` every 15 minutes covers time-based rules and the sales pipeline. One task per rule key ever: open tasks follow the data (title, due time, trip owner), close themselves with a reason when no longer needed, reopen if the need returns; a task a person closed is never re-created. Today view: overdue / within 2 h / later today / tomorrow / next 7 days / no date, priority within each; mine vs everyone; done with a note, snooze (+1 h, tomorrow 09:00), take; manual tasks. Rule tasks keep their title and due time (409 on edits); hand assignment sticks. Audit: SYSTEM rows for created / auto-closed / reopened, HUMAN rows for edits and rule changes | ☑ | (this commit) | Migration `20260919020000_task_engine` (reverse SQL in header) backfills completedAt and assignee ids; the old scheduler's passport rule is retired (its open tasks are closed with a reason and re-raised by the engine). Classic Tasks & Alerts page still reads the same tasks |
| 3.7 | Driver view (`/driver`, `/api/v2/driver`): new DRIVER role with only `driver:duties`; `requireAuth` fences a DRIVER session to the driver API, `/api/v2/me` and sign-out (everything else, including `/api/data/all` and the team list, is 403) and the SPA sends drivers straight to `/driver` without loading office data. A DRIVER login is linked to one driver record by an admin (Fleet → driver → "Driver app login"; DRIVER-role accounts only, one driver per login, audited, unlinking revokes the sessions). The driver sees only their confirmed duties (next 12 h back onwards; last 30 days tab): time, pickup → drop, vehicle, passenger count, customer-safe instructions, group contact, and — on the tour's departure day — the pickup points with times, map, contact and the names to collect. Never amounts, suppliers, identity numbers, dates of birth, e-mails or office notes. Mobile-first navy-and-gold page with one big next-step button (Acknowledged → On the way → Arrived → On board → Completed, `canMoveDriverStatus`), "Report a problem" (note required → urgent task for the trip's ops owner), completion marks the duty completed; every change audited with the driver as actor | ☑ | (this commit) | Migration `20260919030000_driver_role` adds the enum value. User management can create Driver logins. Office sees the driver status on the Transport tab |

## Phase 4 — Finance
In progress on `claude/travelos-phase-3-41f57b`.

| # | Module | Status | Commit | Notes |
|---|--------|--------|--------|-------|
| 4.0 | Ledger core (`/api/v2/ledger`, **Books** page): chart of accounts seeded from `calc/ledger.ts` (money, customers, suppliers, tax, income, trip costs, office, owner — tax accounts carry "verify with CA"), balanced double-entry postings with JV display numbers, `postEntry()` for other finance modules to call inside their own transaction, manual journal entries, reversal-only corrections (a reversal cannot be reversed; the original keeps its number and is marked), trial balance with the difference shown, period profit, account statement with opening balance and running total, journal filtered by account/trip/party/source/text. Every posting audited | ☑ | (this commit) | Migration `20260920000000_ledger` (reverse SQL in header). Nothing posts to the ledger yet — receipts, supplier bills and expenses follow in 4.1–4.3 |
| 4.1 | Customer receipts and refunds (`/api/v2/receipts`, **Receipts** page, money panel on every booking): recorded against the family party's booking where there is one, otherwise against the trip; each row posts to the ledger in the same transaction (cash/bank/gateway against customer advances) and cancelling reverses that posting instead of deleting anything. Refunds cannot exceed what that party paid. Instalment statuses and the party balance now come from the family's own receipts, so `paymentTracking: TOUR` is gone and shared tours show real paid/overdue per family. The trip's cached paid/balance columns follow the receipts, so the classic screens agree. Day book of today's collections by method. Classic customer payments are imported once each (`legacy.payments-import`, hourly and on demand); a payment method the old screen never recorded lands in "Unsorted receipts" (1030) rather than being guessed | ☑ | (this commit) | Migration `20260920010000_customer_receipts` (reverse SQL in header). Supplier payments in the classic table are left for 4.2 |
| 4.2 | Supplier bills and payments (`/api/v2/payables`, **Supplier money** page): a bill books its cost to the right expense account by category (hotel / transport / tickets / activities / other trip / office), holds any GST as input credit ("verify with CA") and owes the supplier; one bill number per supplier. Payments settle a bill (never more than is left on it) or sit as an advance in "Advances to suppliers" until a bill arrives, and can then be applied to it. Aging by how late each bill is (not due / 1–30 / 31–60 / 61–90 / 90+), worst supplier first, plus a per-supplier statement. Cancelling a bill or payment reverses its posting and keeps the row; a bill with payments must have them cancelled first. Classic payables are imported once each — the advance and, where the old screen marked it paid, the remainder become payments with the method recorded as "not recorded". Classic supplier payments in the payments table carry no supplier id, so they are left alone and reported | ☑ | (this commit) | Migration `20260920020000_vendor_bills` (reverse SQL in header). Trip costs from bookings stay as estimates; bills are what was actually charged (profitability compares them in 4.5) |
| 4.3 | Expenses (`/api/v2/expenses`, **Spending** page): money paid directly — fuel, tolls, parking, food on the road, other trip spending, salary, rent, office, bank charges, marketing — each posting to its own expense account, with GST on the bill held as input credit ("verify with CA"). A trip, a party or a supplier can be named on any expense. When a staff member pays from their own pocket the money is owed back to them ("Staff reimbursements", 2400) until an audited reimbursement settles it, and a reimbursement can never be more than is owed. Cancelling reverses the posting and keeps the row. New permissions: `expenses:write` for OPERATIONS and ACCOUNTS (the people who actually spend), `expenses:read` also for BOOKING; only finance may settle reimbursements | ☑ | (this commit) | Migration `20260920030000_expenses` (reverse SQL in header). Receipt photos attach through the documents API (`entityType: expense`) |
| 4.4 | Tax rates as data (`/api/v2/tax-rules`, **Settings → Tax rates**): a catalogue in `calc/tax.ts` (GST on tour packages, GST on the ticketing service fee, GST on a hotel-only booking, TCS on overseas packages with its yearly limit), each with a note saying where the number comes from and what to verify with the CA. A rate change is a new row from a day, so older bills keep the rate that applied then; reading is open to anyone who prices work, changing is admin-only and audited. Quotations and ticket service fees now take their rate from the rules (a rate given by hand still wins); the old `taxes.ticketServiceFeeGstPct` organisation setting is carried into a rule by the migration and `core/settings.ts` is gone. Pure maths for GST splitting (CGST/SGST within the state, IGST outside), tax-inclusive prices, TCS above the yearly limit and April–March financial years | ☑ | (this commit) | Migration `20260921000000_tax_rules` (reverse SQL in header, carries the old setting over). Applying TCS to invoices lands with invoices in the books (4.5) |
| 4.5 | Invoices in the books: issuing an invoice raises customer dues and recognises the sale, output GST and — on an overseas package, only when the TCS rule is switched on and only above that customer's yearly limit — TCS; money already received for the trip moves out of "Customer advances" against the invoice, so the balance shown is what is really left. Cancelling or deleting an invoice reverses both postings; credit notes take the sale and GST back, debit notes charge more, and cancelling those reverses too. TCS appears on the printed invoice. Decimal columns are returned as numbers so the classic screens keep working | ☑ | (this commit) | Migration `20260921010000_invoice_tcs` (additive). The GST invoice numbering, receivables and classic screens are untouched |
| 4.6 | Finance read models (`/api/v2/finance`): **what customers owe** — money received now settles invoices already issued before it waits as an advance, and the board applies what has been received to the oldest invoice first, ages each remaining one (not due / 1–30 / 31–60 / 61–90 / 90+), shows money received but not yet billed, and appears as the "Owed to us" tab beside Receipts. **What a trip made** — billed (invoices, or the bookings' value before anything is invoiced, and it says which), cost actually recorded (supplier bills and spending) against what the bookings planned, margin and percentage, and what is still to collect; shown on the trip workspace for roles with commercial access. Both are derived from the ledger, so they can never disagree with the books | ☑ | (this commit) | No new tables. Aging uses each invoice's due date, falling back to its date |
| 4.7 | **Money** page (`/api/v2/finance/summary`, ADMIN + ACCOUNTS): billed, spent and profit for a period (the financial year by default), cash and bank in hand, what customers owe and what was received but not yet billed, what is owed to suppliers and back to staff, the GST position (charged less input, "verify with CA") and TCS held, a month-by-month bar chart, and the period's trips ranked by margin. Every figure is read from the ledger, so this page, the trip workspace and the books cannot disagree | ☑ | (this commit) | The cached `trips.paidAmount` / `balanceDue` columns are **kept** (decision P4-16): the classic screens still read them, and they now have one writer that follows the receipts |
| 4.8 | Automation: overdue payment → task, reminder draft | ☐ | | |

## Phase 5 — Document intelligence
☐ Document centre · ☐ `AiProvider` + Claude + Gemini adapters · ☐ Evaluation harness (owner's PDFs in `eval-docs/`) · ☐ Pipeline: classify → extract → match → review → apply

## Phase 6 — Copilot · Phase 7 — Comms + automation · Phase 8 — Portal · Phase 9 — Analytics + platform
☐ (Phase 9 also: RBAC tables and custom roles, per-organisation numbering without column defaults, organisation onboarding)

## Requests for the owner
- A Neon branch connection string (or a `NEON_API_KEY`) if you want schema work rehearsed on Neon as well as on the local container. Not blocking: the local container covers every automated check.
- 5–10 real ticket / supplier-invoice PDFs in `eval-docs/` for the Phase 5 provider comparison.
- At cutover: follow `docs/travelos/RUNBOOK-production-cutover.md` (snapshot → catch-up SQL → baseline → verify → deploy). The tenancy migration is part of the chain the runbook applies.
